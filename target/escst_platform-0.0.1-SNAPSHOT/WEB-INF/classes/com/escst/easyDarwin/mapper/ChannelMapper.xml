<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.escst.easyDarwin.mapper.ChannelMapper">
	<select id="selectChannelList" parameterType="String" resultType="com.escst.easyDarwin.entity.ChannelEntity">
		SELECT
			id,
			construction_id as constructionId,
			construction_name as constructionName,
			channel_name channelName,
			channel_no channelNo,
			serial,
			status,
			rtmp_url as rtmpUrl,
			http_url as httpUrl
		FROM
			t_easy_channel t
		 where t.construction_id=#{constructionId}
	</select>
	
</mapper>